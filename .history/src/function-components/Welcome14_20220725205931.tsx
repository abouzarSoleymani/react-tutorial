 interface Todo {
    id: string
    bodyText: string
    completed: boolean
  }

interface Props {
    todo: Todo
  }

// eslint-disable-next-line no-undef
const Welcome14: React.FC<Props> = ({ todo }) => {
    return(
        <>
        </>
    )
}
export default Welcome14